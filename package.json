{
  "name": "zps-ad-intelligence",
  "version": "1.0.0",
  "private": true,
  "description": "ZPS Ad Intelligence — Mobile ad intelligence dashboard powered by Sensor Tower API",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Static frontend — no build step'",
    "start": "vercel dev"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.1"
  },
  "devDependencies": {
    "vercel": "^41.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
